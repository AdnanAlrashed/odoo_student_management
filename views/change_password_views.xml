<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_change_password_wizard_form" model="ir.ui.view">
            <field name="name">change.password.wizard.form</field>
            <field name="model">student_management.change.password.wizard</field>
            <field name="arch" type="xml">
                <form string="Change Your Password">
                    <group>
                        <field name="current_password" password="True"/>
                        <field name="new_password" password="True"/>
                        <field name="confirm_password" password="True"/>
                    </group>
                    <footer>
                        <button name="action_change_password" type="object" string="Change Password" class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_change_password_wizard" model="ir.actions.act_window">
            <field name="name">Change Password</field>
            <field name="res_model">student_management.change.password.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>
    </data>
</odoo>
